[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "gnss-core"
version = "0.1.0"
description = "Rust-based GNSS processing core with RINEX parsing and PPK algorithms"
requires-python = ">=3.8"

[tool.maturin]
module-name = "gnss_core"
