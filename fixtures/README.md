# Smoke fixtures

- `sample.rinex` — tiny observation header fixture
- `sample.dxf` — minimal LINE entity fixture
- `sample.kml` — tiny KML placemark fixture

Use `sample.kml` for manual smoke checks; KMZ can be generated from it if needed.
